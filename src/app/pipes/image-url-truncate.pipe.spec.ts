import { ImageUrlTruncatePipe } from './image-url-truncate.pipe';

describe('ImageUrlTruncatePipe', () => {
  it('create an instance', () => {
    const pipe = new ImageUrlTruncatePipe();
    expect(pipe).toBeTruthy();
  });
});
