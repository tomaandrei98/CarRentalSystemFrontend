import { TableOverflowPipe } from './table-overflow.pipe';

describe('TableOverflowPipe', () => {
  it('create an instance', () => {
    const pipe = new TableOverflowPipe();
    expect(pipe).toBeTruthy();
  });
});
